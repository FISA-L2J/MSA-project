apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: msa-project
  labels:
    app: order-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
    spec:
      containers:
      - name: order-service
        # Replace [PROJECT_ID] with your actual GCP Project ID
        image: asia-northeast3-docker.pkg.dev/msa-project-prod/msa-repo/order-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: "prod"
        - name: SPRING_DATASOURCE_URL
          value: "jdbc:postgresql://postgres:5432/order_db"
        - name: SPRING_DATASOURCE_USERNAME
          valueFrom:
            secretKeyRef:
              name: msa-secrets
              key: postgres-user
        - name: SPRING_DATASOURCE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: msa-secrets
              key: postgres-password
        - name: MANAGEMENT_ZIPKIN_TRACING_ENDPOINT
          value: "http://zipkin:9411/api/v2/spans"
        - name: PAYMENT_SERVICE_URL
          value: "http://payment-service:8081"
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: msa-secrets
              key: jwt-secret
---
apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: msa-project
spec:
  selector:
    app: order-service
  ports:
  - port: 8080
    targetPort: 8080
  type: ClusterIP
